#include <stdio.h>
#include <stdlib.h>

int main()
{
    int faltu;
    faltu=10;
    if(faltu>20)
        printf("Greater than 20");
    else
        printf("Equal or Smaller than 20");
    return 0;
}
