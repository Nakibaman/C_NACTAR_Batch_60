#include <stdio.h>
#include <stdlib.h>

int main()
{
    long int a;
    char c;
    float b;
    double f;
    printf("Interger size is %d",sizeof(a));
    printf("\nCharacter size is %d",sizeof(c));
    printf("\nFloat size is %d",sizeof(b));
    printf("\nDouble size is %d",sizeof(f));
    return 0;
}
